buildscript {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    dependencies {
        classpath 'com.palantir.gradle.jdks:gradle-jdks-settings:0.71.0'
    }
}

apply plugin: 'com.palantir.jdks.settings'

rootProject.name = 'better-exec-root'

include 'better-exec'

